name: demo_functions_app
description: A demo app with 7 Flutter functions (SliverAppBar, GestureDetector, Draggable etc.)
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  # Хэрвээ зураг, icon, json ашиглах бол энд assets жагсаана:
  # assets:
  #   - assets/images/
  #   - assets/data/
